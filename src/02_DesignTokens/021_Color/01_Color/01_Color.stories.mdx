import { Meta, Canvas } from '@storybook/addon-docs';

import BrandColors from '../BrandColors/BrandColors';
import ActionColors from '../ActionColors/ActionColors';
import Gradients from '../Gradients/Gradients';
import Opacity from '../Opacity/Opacity';
import Shades from '../Shades/Shades';
import Tints from '../Tints/Tints';

<Meta title="Design Tokens/Color" parameters={{ previewTabs: { canvas: { hidden: true } } }} />

<h1>Color Design Tokens</h1>
<p>Design tokens are design decisions, represented as data, that ensure systematically unified and cohesive product experiences.</p>
<p>TODO: Need to change loop through each `BrandColor` and `ActionColor` in `Opacities`, `Shades` and `Tints`.</p>

<BrandColors />
<ActionColors />
<Gradients />
<Opacity />
<Shades />
<Tints />